# ═══════════════════════════════════════════════════════════════════
#  APEX Revenue Engine v2 — environment
#  Copy this file to ".env" and fill in the real values.
#  NEVER commit the real .env.  (Add ".env" to .gitignore.)
# ═══════════════════════════════════════════════════════════════════

# --- Resend (email delivery) --- REQUIRED
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
FROM_EMAIL=Kairyx Security <security@bugreaper-x.ca>
REPLY_TO=kairyx@bugreaper-x.ca

# --- Supabase (lead tracking) --- REQUIRED to email real leads
# Project: "Vanthryx empire"
SUPABASE_URL=https://npxpshffkpbqroxegvsj.supabase.co
# ⚠️  USE THE SERVICE ROLE KEY — NOT the anon key.
# RLS is enabled on leads/outreach_log with policies for service_role only,
# so the anon key will silently return ZERO leads.
# Supabase Dashboard → Project Settings → API → service_role (secret).
SUPABASE_KEY=eyJhbGciOi...SERVICE_ROLE_SECRET...

# --- Offer / brand ---
BOOKING_URL=https://cal.com/kairyx/security-snapshot
COMPANY_NAME=Kairyx Security Solutions
SENDER_NAME=Kairyx Security
WEBSITE=https://bugreaper-x.ca
PHYSICAL_ADDRESS=Fort Saskatchewan, AB T8L, Canada
